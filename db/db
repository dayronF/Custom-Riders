create database CustomRiders ;

use CustomRiders;

create table motorbike (
id int primary key auto_increment not null unique,
branch varchar(100),
model varchar(100), 
cc varchar(100),
price int,
quantity int

);
